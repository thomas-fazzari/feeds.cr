name: feeds
version: 0.1.0

authors:
  - Thomas Fazzari

description: |
  Universal feed parser for Crystal. Supports RSS, Atom, and JSON Feed.

license: MIT

dependencies:
  dateparse:
    github: thomas-fazzari/dateparse.cr

development_dependencies:
  ameba:
    github: crystal-ameba/ameba
